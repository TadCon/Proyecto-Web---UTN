npm init

npm i express
npm i nodemon

agregar scripts:
    "dev": "nodemon index.js",
    "start": "node index.js"

npm run dev

//
Verbos HTTP vistos: get, listen, post, delete, put.
//


//
app.listen(Port, () => {
    console.log(`Servidor corriendo en el puerto ${Port}`);
});
¡Importante! Tener en cuenta las comillas utilizadas (en dirección a la izquierda)
//

//
config.js
3306 es el puerto para sql server

Requerir MySQL: Copiar y pegar desde npm mysql2

Añadir mi password de MySQL como atributo del objeto createConnection


//
Después de armar toda la arquitectura, usamos express generator -g
Se instala globalmente en la consola (o Git Bash)


luego escribimos 'express myapp' y se generaron todos los archivos